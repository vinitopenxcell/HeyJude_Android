<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.heyjude.androidapp.activity.HomeActivity">

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/my_drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">


            <FrameLayout
                android:id="@+id/content_frame"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginTop="?attr/actionBarSize"></FrameLayout>


            <include layout="@layout/toolbar" />

        </FrameLayout>

        <ListView
            android:id="@+id/lsDrawer"
            android:layout_width="@dimen/scale_230dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/black"
            android:cacheColorHint="@android:color/transparent"
            android:choiceMode="singleChoice"
            android:divider="@drawable/menu_devider"
            android:dividerHeight="@dimen/scale_1dp"
            android:listSelector="@drawable/listdrawer_selector" />
    </android.support.v4.widget.DrawerLayout>
</LinearLayout>
